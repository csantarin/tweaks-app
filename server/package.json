{
  "name": "tweaks-api",
  "description": "Mock API service for tweaks-app",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "json-server": "^0.17.1"
  },
  "scripts": {
    "serve": "json-server db.json --port 4000"
  }
}
